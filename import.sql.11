BEGIN;
DROP TABLE IF EXISTS sra_agents;
CREATE TABLE sra_agents AS
SELECT SOURCE,
       substr(agent,
              0,
              40) AS agent,
       sum(bytecount) AS bytes
  FROM cloud_sessions
 GROUP BY SOURCE,
          agent
 ORDER BY bytes DESC
 LIMIT 100;
COMMIT;